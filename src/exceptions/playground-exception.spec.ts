import { PlaygroundException } from './playground-exception';

describe('PlaygroundException', () => {
  it('should be defined', () => {
    expect(new PlaygroundException()).toBeDefined();
  });
});
